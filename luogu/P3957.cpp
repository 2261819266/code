#include <iostream>
#include <vector>

using std::cin;
using std::vector;

int n, d, k;

void check() {
    
}

vector<int> x, s;

void P3957() {
    cin >> n >> d >> k;
    for (int i = 0; i < n; i++) {
        int X, S;
        cin >> X >> S;
        x.push_back(X);
        s.push_back(S);
        
    }
}

int main() {
    P3957();
    return 0;
}
